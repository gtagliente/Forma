# docs/branches/

## Purpose

Branch-scoped, temporary context for this repo's own git branches — mirrors `Forma.Claude`'s `docs/branches/` pattern. Anything durable discovered on a branch should be promoted to `../product/`, `../architecture/`, or `../../.claude/agents/` before that branch's context is considered closed.

## Current state

One branch documented: `main/` — this bootstrap work (`docs/` structure, `CLAUDE.md`, subagents) happened directly on `main`, same as `training-planning-service`'s bootstrap — this subfolder had no prior branching convention established either. See `main/context.md`.
